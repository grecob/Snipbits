<Window x:Class="SnippetManager.Views.MainView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:SnippetManager"
        Title="Code Snippet Manager" Height="600" Width="800"
        ResizeMode="CanResize">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="3*"/>
        </Grid.ColumnDefinitions>

        <!-- Snippet List -->
        <ListView ItemsSource="{Binding Snippets}" 
                  SelectedItem="{Binding SelectedSnippet}"
                  Margin="5">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="Title" DisplayMemberBinding="{Binding Title}"/>
                    <GridViewColumn Header="Language" DisplayMemberBinding="{Binding Language}"/>
                </GridView>
            </ListView.View>
        </ListView>

        <!-- Editor Area -->
        <Grid Grid.Column="1" Margin="5"
              MinWidth="80"
              MinHeight="200">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <StackPanel Orientation="Horizontal">
                <Button Content="New" Command="{Binding NewSnippetCommand}"/>
                <Button Content="Save" Command="{Binding SaveSnippetCommand}"/>
                <Button Content="Delete" Command="{Binding DeleteSnippetCommand}"/>
            </StackPanel>

            <TextBox Grid.Row="1" Text="{Binding SelectedSnippet.Code, Mode=TwoWay}"
                     AcceptsReturn="True" TextWrapping="NoWrap"
                     FontFamily="Consolas" FontSize="14"
                     VerticalScrollBarVisibility="Auto"
                     HorizontalScrollBarVisibility="Auto"/>
        </Grid>
    </Grid>
</Window>